# HHKB Layer 2

Layer 1은 변경하지 않는다.

```text
Esc → Esc

1 → !   2 → @   3 → #   4 → $   5 → %
6 → ^   7 → &   8 → *   9 → (   0 → )
- → _   = → +   \ → \   Backspace → Backspace

Tab → Esc
Q → q   W → w   E → e   R → r   T → t
Y → y   U → u   I → i   O → o   P → p
[ → [   ] → ]

A → =   S → `   D → -   F → \   G → [
H → ←   J → ↓   K → ↑   L → →
; → :   ' → '

Z → +   X → !   C → %   V → &   B → (
N → )   M → *   , → <   . → >   / → ?

Enter → Enter
Space → Space
```

Ctrl, Shift, Option, Command, Fn 등 나머지 키는 기존 Layer 2 값을 유지한다.
